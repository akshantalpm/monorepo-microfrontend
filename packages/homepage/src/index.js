import React from 'react';
import ReactDOM from 'react-dom';
import HomePage from "./components/HomePage";

const root = ReactDOM.createRoot(document.getElementById('root-homepage'));
root.render(
  <React.StrictMode>
    <HomePage />
  </React.StrictMode>
);
