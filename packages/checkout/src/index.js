import React from 'react';
import ReactDOM from 'react-dom/client';
import CheckoutPage from "./components/CheckoutPage";

const root = ReactDOM.createRoot(document.getElementById('root-checkout'));
root.render(
  <React.StrictMode>
    <CheckoutPage />
  </React.StrictMode>
);
