import React from 'react';
import ReactDOM from 'react-dom';
import ProductPage from "./components/ProductPage";

const root = ReactDOM.createRoot(document.getElementById('root-productpage'));
root.render(
  <React.StrictMode>
    <ProductPage />
  </React.StrictMode>
);
